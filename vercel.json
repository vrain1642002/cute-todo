{
    "installCommand": "if [ ! -d $HOME/flutter ]; then git clone https://github.com/flutter/flutter.git -b stable --depth 1 $HOME/flutter; fi && $HOME/flutter/bin/flutter doctor && $HOME/flutter/bin/flutter pub get",
    "buildCommand": "$HOME/flutter/bin/flutter build web --release --web-renderer auto",
    "outputDirectory": "build/web",
    "framework": null,
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/index.html"
        }
    ]
}